let val;

/*
	We can do Math Operations 

	Adding (+)
	Subsctraction (-)
	Multiplication (*)
	Division (/)
	Modules(%)
*/

val = 2 * 5;
val = 2 + 5;
val = 2 - 5;
val = 2 / 5;

// console.log(val)

/*
	Math Objs

	Powerof (pow)
	RoundNum(round())
	PI
	E
*/
val = Math.PI;
val = Math.E;
val = Math.round(2.8);
val = Math.ceil(2.8); // 3
val = Math.floor(2.8); // 2
val = Math.sqrt(64); // 8
val = Math.abs(-64);
val = Math.pow(8, 2);
val = Math.min(1,23,456,89,-9,);
val = Math.max(1,23,456,89,-9,);
val = Math.random();
val = Math.ceil(Math.random()*20+1);
console.log(val)